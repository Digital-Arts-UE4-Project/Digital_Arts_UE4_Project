追加したコンポーネント等の数値設定

C_ReturnCollision（コリジョン）:高さ、サイズはコンストラクタで変えてるのでデフォのままでOK

PlayerLength(関数)エレベーターとプレイヤーの高さを図る関数

BaseLength(Float変数）:高さをはかる際に基準となる数値。値は200

RCollisionSize(Float変数):コンストラクタで設定するC_ReturnCollisionのx,yのサイズ。値は8。